# 文件格式規範

本 repo 所有 Markdown 文件共用的格式規則。新增或修改文件時請依照本文件；現有文件都已經整理成這個格式。

## 1. 通用規則

適用於 repo 內所有 `.md` 文件。

### 1.1 標題

- 整份文件**只有一個 H1**，而且是檔案第一行（前面不留空行）。
- 章節用 H2，一律編號：`## 1. 名稱`。子章節用 H3：`### 1.1 名稱`。更深一層用 H4，可以不編號。
- 只用阿拉伯數字編號，不用「一、二、」「第一章」。編號後面固定一個空格（`## 3. CFS`，不是 `## 3.CFS`）。
- 編號連續，不跳號、不重複。
- 標題內不加粗體（`**`）、不加 emoji，`#` 後面只有一個空格，行尾不留空白，結尾不加冒號。
- 同一層的標題不要重複；內容不同就用括號補充區分，例如 `Debug Flow（逐層判斷）`。
- 不要用 H1 當章節，也不要用純文字或粗體行假裝標題；要當標題就用 `#`。
- 內文引用章節時寫編號（例如「見 §3.2」），重新編號時要一併更新。

### 1.2 內文

- 不使用 emoji。需要表示是 / 否時用 `✓` / `✗`，需要提醒時寫「注意：」。
- 流程圖的箭頭（`→ ↓ ←`）、樹狀圖的框線字元（`├─ └─ │`）不受此限。
- 章節之間不加分隔線（`---`、`----------`），用標題區隔即可。
- 不留連續空行，空行裡也不要有空白字元。
- 使用繁體中文，不要混用簡體字。
- 不寫手動目錄，GitHub 會自動產生。

### 1.3 程式碼區塊

- 多行的指令、log、流程圖、呼叫樹一律用 ```` ``` ```` 圍起來，不要用單一反引號跨行。
- 每個 ```` ``` ```` 都要成對，檔案結尾不能留著沒關的區塊。
- 呼叫樹要保留縮排，讓層級看得出來：

```text
load_module()
├─ layout_and_allocate()
└─ do_init_module()
   └─ do_one_initcall()
```

- 寫 kernel 呼叫路徑時，對照實際原始碼，並註明是以哪個 kernel 版本為準。
- 每個程式碼區塊都要加語言標籤，寫法是 ```` ```bash ````（反引號和語言之間不留空格）。語言標籤依內容選：指令用 `bash`，C 程式碼用 `c`，Device Tree 用 `dts`，流程圖、呼叫樹、log、kernel 參數用 `text`。其他語言用對應的標籤（`ini`、`diff`、`mermaid` 等），不確定就用 `text`。

### 1.4 表格與清單

- 表格內的行內程式碼如果含有 `|`（例如 `dmesg \| grep PM:`），要寫成 `\|`，否則 GitHub 會把它當成欄位分隔。
- 不要用一連串沒有內文的標題當清單，改用 `-` 或 `1.` 清單。

### 1.5 檔案

- 檔名要有 `.md` 副檔名，GitHub 才會渲染。
- 新檔名用小寫英文加連字號，例如 `imx8mp-brcmfmac-p2p-analysis.md`。現有檔名不必改。

## 2. 除錯 / 分析報告（cases-study/）

`cases-study/` 放實際遇到的問題紀錄，分兩種模板。

### 2.1 模板 A：除錯 / 分析報告

```text
# <平台> <主題>

## 1. 問題概述
## 2. 系統環境
## 3. 除錯過程        （純技術分析類可用「分析過程」）
## 4. Root Cause 分析
## 5. 解決方案
## 6. 上游狀態
## 7. 結論與建議
## 附錄
```

### 2.2 模板 B：操作指南 / 教學

```text
# <平台> <主題>

## 1. 目標
## 2. 環境與前置條件
## 3. <步驟>          （3 ~ N 為步驟，每個步驟一個 H2）
## N+1. 驗證
## N+2. 常見問題與排查
## 附錄
```

### 2.3 規則

- H1 以平台開頭，例如 `i.MX8MP`、`RZ/T2H`、`RK3588`、`Raspberry Pi 5`。
- 模板章節名稱固定；需要補充時加在括號裡，例如 `## 5. 解決方案（commit 對照）`。
- 沒有內容的章節直接省略，編號照實際章節連續排（沒有「系統環境」時，「除錯過程」就是 2.）。
- 不屬於任何模板章節的內容，放到最合適的章節底下當 H3。
- 同一份報告有多個獨立問題時，在 3 / 4 / 5 底下各用一個 H3 分開（例如 `### 3.1 問題 1：...`）。
- 附錄不編號：`## 附錄`，底下用 `### A.`、`### B.`。常用指令摘要、Checklist 放附錄。

## 3. 概念筆記（notes/、bcmdhd/、bluetooth/、mt76/、trace-notes）

這類筆記的主題章節每份都不同，不套固定章節名稱，只統一外框：

```text
# <主題>

<簡介段落，直接接在 H1 下面，不加標題>

## 1. <主題章節>
### 1.1 <子章節>
## 2. ...
## N. 常見問題與排查      （選填）
## N+1. 學習建議          （選填）
## 附錄                   （選填）
```

- 「本章目的 / 本章定位 / 前言 / Introduction」這類只在介紹本文的段落，不加標題，直接放在 H1 下面。如果前言本身在講技術內容，就當作 `## 1.`。
- 文件後段講常見錯誤、陷阱、誤判、失敗模式、Debug Checklist 的章節，統一命名為 `常見問題與排查`。
  - 原標題比「常見錯誤」更具體時，放在括號裡保留：`## 7. 常見問題與排查（Deadlock 常見原因）`。
  - 有多個這類章節時，合併到同一個 H2 底下，各自當 H3，保留原標題。
- 「學習建議 / 學習與觀察建議」統一為 `學習建議`。
- 參考資料、延伸閱讀、指令速查表放 `## 附錄`，底下用 `### A.`。
- 檔名含 `debug-playbook` / `debugging-playbook` 的文件整份都是除錯內容，不套「常見問題與排查」的命名規則。
- mt76 系列的 H1 保留「第 N 章：...」，章內編號從 1 開始。

## 4. README

- 根目錄的 `README.md` 是 repo 首頁，用英文撰寫，章節不編號，其他通用規則照舊。
- 以下規則適用於 `code/examples/` 底下的範例 README。
- H1 用範例名稱，例如 `# drm_simple_kms`。
- 「本章的目的 / 本範例的目的」直接放在 H1 下面，不加標題。
- 其餘章節依第 1 節規則編號；「Kernel 原始碼對照」「Build / Run」這類章節維持原位。
- 結尾的常見錯誤 / 常見誤解章節依第 3 節規則命名為 `常見問題與排查`。

## 5. 自動產生的檔案

- `assets/notes_index.json` 由 [`tools/generate_index.js`](tools/generate_index.js) 產生，每次 push 到 main 時 CI 會自動重建並 commit，不需要手動修改或在本機產生。
